---
title: bolloon-UI 当前状态
source: session
created: 2026-09-09
last_confirmed: 2026-09-24
audience: self
stage: draft
tags: [status, privacy-policy-page, footer-compliance, icons-store-specs, on-chain-activity, scoped-counts, copy-lean, pulse-tasks-fixed-height-paging, pulse-activity-15row-paging, ctl-copy-clarity, bolloon-lime-controls]
status: current
---

## 最近更新

- (2026-09-24) **链上活动表：十五行高度上限 + 底部十五行/页分页栏（+ 控件文案/配色一轮）· 断言 370 → 401 只加不减 · 变异验证真判红 5 条后恢复全绿 · v=31 · 已部署 CF Pages**：leo「链上索引 · 全量 · 15 行 … 可以按十五行的高度来设计吗」+「分页栏切换合约区块记录在十五行底部」（clarify：分页 = 下一页）+「切换也太模糊，颜色换一下 bolloon 色系」→ ① **上限取实测真高度**：1440px = caption 30.69 + 表头 41.19 + 15×52.84 = **865.03px**（窄屏 77.38 行高 → **1233px**），写成 `--pulse-activity-h`；新增内层滚动层 `.pulse-table-scroll`（`max-height` + `overflow:auto`），≤15 行贴内容高、>15 行**框内滚**，表头 `sticky` + 不透明底；**分页栏**（`data-pulse-activity-ctl`）贴十五行底部，一页 = `data-pulse-activity-page="15"`（= 上限那份行数 ⇒ 翻页 = 换一屏），页数从表里行数算；只要有行就始终显示（没行才整行隐藏）；超 60 行上限被截掉的部分补「另 R 行未列」（60+10=70 与口径行不打架）。② **文案去模糊**：分栏按钮写**动作 + 去向**（切到双栏 / 切回单栏）、翻页写上一页/下一页，每个按钮带同源 `title` + 含可见文案的 `aria-label`，**禁用时也写明为什么按不动**（已经是第一页/最后一页）。③ **配色**：`.pulse-ctl-btn` 从灰字灰边改**品牌 lime**（文字/描边/极淡底），悬停反白、按下实心 lime、禁用退回弱色、键盘焦点 lime 环。**真截图复核补一刀**（v=30 → v=31）：线上 1440px 全页截图逐段放大后发现第一版把**禁用态写成灰字灰边**，而真快照正好 15 行 = 一页 ⇒ 两个翻页按钮**长期禁用**，这条栏在线上永远是一片灰（bolloon 色系根本看不见）→ 改成**暗调 lime + 虚线边**，并加门断言（禁用 = `--lime-deep` 虚线、可点 = 亮 lime 实线）防回归。④ **门 [15] + 变异验证**：「内容超上限就在框内滚」这条在真数据上量不出来（15 行 = 一页正好一屏），改成**在页面里把行临时摞到 60 行**直接量（量完删干净，DOM 回到原样），并同批量「拿掉上限会被撑到 3243px」证明**上限是载荷的**；把 `max-height` 改 `none` 走一遍 → **396/5**（红的正是那 5 条），恢复后 **401/0/0**。**未做**：真快照正好 15 行 = 一页 ⇒ 线上两个按钮都禁用，「翻页真换行」只在夹具上验；`bolloon.pages.dev` 之外的第二通道按前一轮拍板未加；两仓未 push（按约定留主线）。
- (2026-09-24) **待接单任务条目：固定高度 + 下滑滚动 + 分页 + 分栏切换（断言 348 → 370 只加不减 · 变异验证真判红 5 条后恢复全绿 · v=28 → v=29 · 已部署 CF Pages `d85a27c6.bolloon.pages.dev` · 线上 370/0/0）**：leo「这个任务条目你设置一下固定高度，就这个目前的高度就可以，以后可以下滑滚动查看，分页分栏切换」→ ① **固定高度**取一行 chip 的**实测值**（1440px **40.69px**、390px 换行后 **61.38px**、首页紧凑版 24.47px），写成 CSS 变量 `--pulse-task-row-h`，列表容器 `height: var(...)` + `overflow-y:auto` —— **条目再多这一块一像素不长**，多出来的框内滚；② **分页**每页 `data-pulse-tasks-page="4"`（网关页声明、app.js 读），页数从快照条数算，控件（上一页 / 「第 x/y 页 · 共 N 条」/ 下一页 / 分栏）只在条数 > 一页时出现，一页装得下就整行隐藏（真快照 1 条 → 页面上没这东西）；③ **分栏**切 `is-cols2` 双栏、`aria-pressed` 记状态、文案随状态走（分栏 ↔ 单栏，该按钮故意没有 `data-zh/data-en`——动态文字归 app.js）；④ 「+N」仍只数上限之外被截掉的条目，**与分页口径不混**。门 `[14]`：先用**真数据**量基准，再注入新夹具 `FX_TASKS_MANY`（7 条）验分页/分栏/边界，**每一步都要求列表高逐像素等于基准**；390px 双栏每条都在框内且不贡献横向溢出。**同轮定位的「刷新不出来」根因**：站点有**两条各自算「已发布」的通道** —— CF Pages 与**备案主机阿里云 ECS**（`120.26.82.43`，nginx root `/var/www/bolloon.cn`）；`refresh-pulse.sh` 历来只部署 CF、**从不同步 ECS**，只跑 CF 时走 bolloon.cn 那份仍是上次的快照。leo 拍板「bolloon.pages.dev 的更新就行」→ **采纳减法**：`refresh-pulse.sh` **不加第二通道**，只留一行注释钉住「唯一对外通道 = CF Pages（bolloon.pages.dev），备案主机不在验收范围，别再往这个脚本里加第二通道」（一次刷新只认一个「已发布」，免得再出现「A 通道新、B 通道旧」的假绿）。**未做**：备案主机按要求不同步（本机也无可用凭据：`Permission denied (publickey)`，无密钥/密码落盘）；两仓未 push（按约定留主线）。
- (2026-09-24) **顶部计数行「已验证/未接入」槽整体下线（两页）· 断言 341 → 348 只加不减 · v=27 → v=28 · 已部署 CF Pages `4442da1c.bolloon.pages.dev` + 备案主机 ECS（两处验收均 348/0/2）**：leo 问「链上活动页, 未接入是什么意思, 可以去掉吗」—— 答: 那一格是**快照标 `unavailable` 的计数槽**的占位文案; 而「已验证」在链上索引里**没有第四类事件可报**（任务创建 / 任务接下 / 任务完成 · 交易结算之外没有「验真」），**无数据源** ⇒ 该槽**整体去掉**（不是改成 0 / 隐藏 / 换个词）。① **网关页小结行 7 项 → 6 项**（节点 / 智能体 / 任务 / 已完成 / 已结算 / 钱包签名），删的是那一格的 `<li>` 本体（`data-pulse-total="tasks_verified"` + `data-pulse-scope-tag="tasks_verified"`）；**首页 `index.html` 本来就只有 6 格**，markup 未动；`app.js`/`style.css` **只补注释**（三态渲染逻辑一字未改 —— hook 在 markup 里不存在 ⇒ 永远渲染不出来 = 「连钩子都没有」，不是显示 0 也不是 —）。② **快照契约一字未改**（导出器没动）：`totals.tasks_verified` 仍为 `null`、`totals_scope.fields.tasks_verified` 仍带 `source/unavailable/short/label` —— **页面不显示 ≠ 假装这个字段不存在**。③ **新门 `[6e★★★★]`「顶部计数区不再出现『未接入』」七条**：两页**静态 HTML** 该格钩子各 0 处（前置证明小结行 markup 真在 —— 缺失类断言不许在空输入上恒真）· 两页**真 DOM** 计数行整块可见文本 0 次「未接入 / not connected」· 计数行仍**六格**且键逐字对上（不许顺手删别的、不许把这一格塞回来）· 快照契约仍在。既有断言**按新事实改写不删不弱化**：「真快照应显示未接入」→「槽已不在」· 缺字段夹具隐藏行数 5 → 4 · `contradictionFindings` 规则⑤「必须写未接入」收窄为**只对页面上真有这个槽的字段生效**（槽缺席由新门守）。④ **变异验证（阴性对照）**：把那一格塞回 `gateway.html` → **337 passed / 11 failed**，新门三条真判红且载荷点名（静态钩子 `{total:1,tag:1}` · 真 DOM 实测文本含「**未接入 已验证 未接入**」· 六格键实测含 `tasks_verified`）；删掉注入后复跑 **348 passed / 0 failed / 2 skipped**。⑤ **部署 + 真 DOM/HTML 验收**：CF Pages `4442da1c.bolloon.pages.dev`（`gateway.html` sha256 `b691d0ed…` 与本地逐字节一致）→ **348/0/2（EXIT=0）**；备案主机 = 阿里云 ECS `120.26.82.43`（nginx root `/var/www/bolloon.cn`，**bolloon.cn 按备案要求仍停解析**，故走机内 `--resolve` + 本地 TLS 透传代理把 `http://127.0.0.1:8977` 转给 ECS:443）→ 机内 6 个路径全 **200** 且 sha256 一致，CDP 真浏览器渲染**备案主机那份字节** → **348/0/2（EXIT=0）**；两处 dump-dom 真文本小结行 = `3 节点 · 24h 脉冲 3 智能体 · 24h 脉冲 5 任务 链上索引·全量 3 已完成 链上索引·全量 5 已结算 链上索引·全量 8 钱包签名 24h 签名审计`，**6 个 li、0 次「未接入」**。⑥ **如实记录**：全页**可见文本**仍 2 次「未接入」，均**不在**计数行 —— 快照自身 `notes`（服务端数据原文，改它要动主仓导出器，本轮未动）+「开发者说明」卡片里描述**表格区**降级措辞的文档句（各有既有门要求它们在，本轮未动）。**未做**：两仓按约定**未 push**（留给主线）。
- (2026-09-23) **「待接单任务」脱敏上公开页（快照 `open_tasks[]` · 缓存破坏 v=25 · 本机 320/0 · 真域名 320/0）**：主仓快照新增 `open_tasks[]`（只含**未认领且未过期**的公告，每行**只有白名单 7 键** capability / budget(原子) / currency / network / deadline / claimed / announcementId 前 8 位；任务正文·正文摘要·买方 DID 与公钥·认领者·签名·地址**一个都不导出**），站点只做展示。① **两页各一处极短区块**：网关页 `gateway.html`（口径行之后）+ 首页 `index.html`（hero 紧凑栏），钩子 `data-pulse-tasks` / `data-pulse-tasks-empty`；每 chip = `capability · 预算<原子值> <币种> · <network> · 截止 MM-DD · ann-xxxxxxxx`（`<time datetime>` 带完整 ISO；标题旁短标记「预算 · 原子」——原子单位不替数据换算小数位）。首页紧凑版 `data-pulse-tasks-max="1"` 只列**最近到期**的一条，被上限截掉的用 **「+N」如实计数**。② **空态说真话**：`open_tasks: []` = 「本节点此刻没有待接单任务」→「暂未观察到」/「Empty = none observed」，**不显示假 0**、**不写与事实相反的「尚未接入」**；老快照没有该键 → 整块隐藏，不报错不崩版面。③ **隐私守卫精确化（不是放松）**：`pulse-privacy-check.py` 加 `open_tasks` 检查（行键集合白名单 · 短 id 形状 + 全文 id 拒 · `claimed: true` 拒 · 字段字符集/类型），`test-pulse-guard.sh` 对照 **19 → 29 条全通过**。④ **`verify-site.mjs` 断言只加不减 291 → 320**：渲染后可见文本不得出现 `0x[0-9a-f]{40}` / `did:` / `did:key` / multiaddr 形态 · 空态文案在场且非假 0 · **capability 与预算逐字对上快照实际值（期望值从快照推导，不写死）** · 不得出现任务正文样本串 · 两页都验。⑤ **部署 + 真域名验收**：`deploy-pages.py` → **`d8c14716.bolloon.pages.dev`**；`verify-site.mjs https://bolloon.cn` → **320 passed / 0 failed / 0 skipped（EXIT=0）**（本地同一份门同样 320/0/0）。⑥ **修了两道既有门的环境竞态**（真域名慢启动时「`Page.navigate` 后固定 sleep 就量 `document.body`」会撞上没建好的文档 → 整轮崩而非断言失败）：改成等文档就绪再量，并把「真的量不到」单列成**会判红**的检查（空文档上缺失类断言恒真，不许当作通过）—— 加强不是放宽。**未做**：两仓按约定**未 push**；页面只展示、**没有公开的「点击认领」API**（认领仍在主仓 CLI `bolloon task claim`）；`open_tasks` 只投本机公告板（远端公告不进快照）。
- (2026-09-23) **站点文案精简（删长解释、留最短诚实标记 · 缓存破坏 v=24）**：按 leo「这些内容不用显示，简洁最好」+ 逐字点名的 5 段，把站点上的长解释从**可见文案**里删干净：① `.pulse-caveat`（两页）「观察窗口内计数 · 不是全网精确总量。」→ **「观察窗口 24h」**（并从小结行下方就近标注，位置从页面底部上移）；② 口径行整句（「上方 任务/已完成… 是另一套口径 —— 不是数据丢了」）→ **「链上索引 · 全量 · N 行 / M 个不同任务 · 网络：<chain_id>（<链名>）」**；③ 网关页隐私整句（「…没有 DID、没有 peerId、没有 IP、没有钱包地址、没有任务正文；地址与哈希一律短写。」）→ 从可见文案删除，**整段设计意图搬进 HTML 注释**（注释不算可见文本，断言要求它还在）；④ 表下「链上数据源：链上索引」长前缀删除，数据源短标记按快照字段派生（chain-index → 「链上索引 · 全量」/ pulse-events → 「脉冲事件」/ none → 「本节点未接入链上数据源」/ 快照没读到 → 「本次未读到快照」），口径行已带标记时表下那行留空不重复、老快照没有口径行才回退到那行；⑤ 「智能体私有网站」整句说明删除，空态压成 **「空 = 未发布（不是没数据）」**（空 ≠ 没数据 没被删，也没有假 0；`.pulse-sites-note` 元素已不存在）。**诚实信号一条没丢**：两套口径（24h 脉冲事件 vs 链上索引全量）改成**各自带一个就近短标记**，删长句 ≠ 把口径改大（页面可见文案里「全网总量/精确总量/exact global total」0 命中）。`scripts/verify-site.mjs` **只加不减：15 条旧断言改写 + 31 条新增（净 +16，275 → 291 项）**——要求短标记成对在场、脉冲区文案不写整句（禁「。」「；」「——」+ 长度上限）、7 页可见文案无长句/无夸大措辞、私有站空态 ≤24 字且无假 0。真跑：本机 `http.server 8941` → **291 passed / 0 failed ×2（EXIT=0）**；`--dump-dom` 前后对比逐条留档。**未做**：按约定未 push、未部署（父智能体负责）；服务端 `notes`（快照数据原文）本轮未动。
- (2026-09-22) **链上活动区去假标签 + 口径行上线（缓存破坏 v=21）**：① 删掉**现在为假**的文案 —— `gateway.html`/`index.html` 共 4 处 `公开观察入口尚未接入`（unavailable 态标签 + 提示首句）+ index 顶部注释 → 改为真话「快照暂时读不到」/「快照这次没读到 — 本站的数据源是签名快照 network-pulse.json」（入口**早已接入**并在真供给 25 行链上活动）。② 新增**口径行** `[data-pulse-activity-totals]`（网关页表格下方 + 首页序栏）：把「上方 任务/已完成/已验证/签名（24h 脉冲事件口径）」与「链上索引 N 行」的关系、以及这批行属于哪条链（31337 本机隔离开发链 · **不是公网活动**）写在明面上；数据取自快照 `activity_totals`/`chain_id_scope`/`totals_scope`，老快照缺这三块 → 整行隐藏。③ `scripts/verify-site.mjs` 加防复发断言：[6d′] 0 任务与 25 行同屏必须有解释、[6e] 同源**真快照**当场验、[11b] 7 页原始 HTML + 渲染文本 + 导航都不得出现"尚未接入"类虚假文案。④ 真跑：本机 **226/0**、部署 `f035d62f.bolloon.pages.dev`、真域名 bolloon.cn **226/0**（`verify-privacy.mjs` 回归 38/0）；线上 dump-dom 实文本：25 行 + 口径行逐字在、「尚未接入」0 次。**未做**：GitHub Pages 通道（需 push，按约定未 push）。**2026-09-23 更新**：本条的**口径行整句**与 `.pulse-caveat` 长句已按 leo 要求精简为极短标记（见上一条），断言同步加强（291 项）。
- (2026-09-19) **Android 0.4.28 发布（品牌迁移后首个 APK）**：`com.hibs.bolloon` 新包名下首个正式签名包 `bolloon-0.4.28.apk`（19,915,587 B / sha256 `d339065a6d6fe397…` / versionCode 28 · versionName 0.4.28 / DN CN=Bolloon），走 **full flavor**（官网直装版，保留无障碍 + Shizuku）。发布三件套：GitHub Release `android-v0.4.28-signed`（asset digest 与本地一致）、同域镜像 `bolloon.cn/dl/bolloon-0.4.28.apk`（200 / Content-Type apk / Length 19,915,587）、`install.html` Android 栏目（直链 + 19.0 MB + 版本 + sha + 校验和/备用下载链接）。CF Pages 部署 `46f89480.bolloon.pages.dev`；iOS 入口未动（仍 `ios-v0.4.24-unsigned`）。
- (2026-09-16) **已上线 bolloon.cn + 线上验收全绿**：CF Pages direct-upload 部署 **`390e5d0e.bolloon.pages.dev`**（上传 17 文件 / 129 去重，2.4 s）。自定义域立刻生效（`privacy.html` 200 / 20,047 B / 标题「隐私政策 — bolloon」；响应头 `max-age=0, must-revalidate` → **不需要 purge**；`style.css?v=15` 是新 URL 天然绕过缓存）。线上真浏览器验收：`verify-privacy.mjs https://bolloon.cn` **38/38**、`verify-site.mjs https://bolloon.cn` **23/23**；APK 同域镜像 200 / 19,193,025 B 未受影响。顺带把 `deploy-pages.py` 的 `EXCLUDE_DIRS` 加了 **`.kilo`**（本地 git worktree 副本 6.1 MB，旧部署一直公开着 `https://bolloon.cn/.kilo/`=200；已停止上传并实测该路径现在回首页兜底）。**仍未做**：GitHub Pages 通道（需 push 才构建，按约定未 push）；zone 缓存 purge（wrangler OAuth token 只到 `zone (read)`，无 Cache Purge 权限 —— 本次实测不需要）。
- (2026-09-16) **隐私政策页 privacy.html（应用市场表单要填的那个链接）**：新增 `privacy.html` + 生成器 `scripts/gen-privacy-page.py`（单一来源、可复跑，避免与 App 内摘要/商店表单三处漂移）：七章 + 三张表（数据清单 / 权限 / 第三方服务），中英双语走站点既有 `data-zh/data-en` 机制；事实逐条对着代码写完（无账号体系 · 无统计/崩溃 SDK · 蓝牙 `neverForLocation` · 位置 `maxSdkVersion=30` · 相机不申请 CAMERA · 无障碍与 Shizuku 仅官网直装版 · 境外服务披露 · 注销即时生效 + 最长 7 个工作日 + 链上不可删）。**5 页页脚统一加「隐私政策」链接 + 备案号占位注释块**（备案完成后把注释块展开即公示，链接 beian.miit.gov.cn）；`style.css` 加政策页样式并全站缓存破坏 **v=14 → v=15**。**新增零依赖验收脚本 `scripts/verify-privacy.mjs`（真 Chrome CDP）本地 38/38**（14 个必填要素 + 3 张表 + 中英双语计数一致 + EN 切换真生效 + 5 页页脚链接与备案锚点 + 站内链接全可达 + 无 JS 报错），站点回归 `verify-site.mjs` **23/23** 无退化。
- (2026-09-16) **商店/备案用图标四件套**（`icons/` 与 bolloon 仓 `src/web/icons/` 双目录镜像）：从品牌 master `icon.png`(1254×1254 满幅无圆角) LANCZOS 下采样出 **`icon-1024x1024.png`**(607.4 KB, md5 `779dd53b8e54f83710cceaa029fa5475`) / **`icon-1024x1024.webp`**(15.9 KB) / **`icon-216x216.png`**(30.6 KB) / **`icon-216x216.webp`**(2.6 KB) —— 对齐商店规格（正方形 · 216 或 1024 · PNG ≤3 MB · WEBP ≤100 KB · 无 alpha 满幅），216 档肉眼复核清晰无锯齿。**顺带发现真实不一致**: `ios/App/App/Assets.xcassets/AppIcon.appiconset/AppIcon-512@2x.png`(1024×1024) 与品牌 master 不同源（sha256 `a3d1d11e…` vs `02098983…`）→ 已在 bolloon 仓重出为同源（见该仓 wiki 2026-09-16）。
- (2026-09-15) **入网 skill 文档 v1.2.1 + iOS 0.4.24 包**（`bolloon-gateway-join.md` / `skill.html` / `install.html`）：① 新增 **§0.1 路径 A′「手机端已内置」**（iOS/Android/PWA 点「一键入网」即由**手机本机内核**真执行：读说明校验 frontmatter → 本机 DID → 服务登记[电脑端可达则真进网络 registry，不可达则如实标注] → P2P 公告[无对端如实标"连上即生效"] → 落盘 `bolloon_gateway_join`），标题改「三条执行路径」；② **§6 被委派语义回写为真执行**——严格能力匹配（无匹配如实回 `delegatedTo:null`，**不**兜底挑 `local.agents[0]`）、被委派端真跑 agent 且 `resultCid` 是真 CID（不再 `mock-<ts>`）、超时即 504 不假成功；③ 安装页 iOS 入口改指向新包 **`ios-v0.4.24-unsigned`**（`Bolloon-unsigned.ipa`，10,090,692 B / 9.6 MB）；④ 缓存破坏 v=13 → **v=14**；⑤ `scripts/verify-site.mjs` 断言同步（1.2.1 + 线上正文须含「路径 A′」）。
- (2026-09-15) **入网 skill 文档 v1.2.0 同步**（`bolloon-gateway-join.md` + `skill.html`）：新增 §0.1 两条执行路径（本机是 bolloon 就调工具 `join_global_gateway`，别再照抄 TS 伪码）、§7 重写为「首次接触 TOFU」（`AddressBroadcast` 自携 `publicKey` 且纳入签名覆盖、did:key 做 DID↔公钥派生一致性检查、公钥不一致拒收不覆盖）、§3 注明 `/api/agent` 启动即挂载、§9 排错 +4 行、§10 补 `gateway-join.json`。**版本徽章去硬编码**：5 页内联 `0.4.20` → 占位 `—`，`app.js` 删 `VERSION_FALLBACK`，只认 live 数据（npm registry → GitHub tag，且只接受形如 `0.4.23` 的值），取不到就保持 `—`。部署 CF Pages `1de7518a.bolloon.pages.dev` 并补回同域 APK 镜像（部署前 `dl/` 是空的，先拉回 0.4.22.3 资产再传）。**新增 `scripts/verify-site.mjs`**（零依赖 CDP 真 Chrome 验收）本地 + 线上均 **21/21**。

- (2026-09-13) 产品页「它能做什么」精简为**三条**（P2P 网络 / 微支付信息 / 技能沉淀与互传，序号顺序重排 **01/02/03**）+ 新增**核心能力滚动展示栏**（两份等宽轨道 `translateX(-50%)` 无缝循环，悬停暂停，`prefers-reduced-motion` 下静止）；静态列表排版保持原样。缓存破坏升 **v=13**；CF Pages 重新部署。安装页/文档页仍为九条。
- (2026-09-13) 内容对齐 npm **0.4.21**：产品/安装/文档三页「它能做什么」5 → 9 条（新增 微支付信息 x402 / 人机问答 / 技能沉淀与互传 / 勿扰时钟），文档页新增 `bolloon setup`·`bolloon model`·`bolloon x402 list` 命令板 + 参考表 5 行，安装页启动区补 setup/model 两板；缓存破坏升 **v=12**；CF Pages 重新部署（direct-upload）。bolloon 手机端新增「一键入网 · 全球智能体网络」点按项（默认 prompt = `read https://bolloon.cn/bolloon-gateway-join.md`）。
- (2026-09-10) 发布 Android APK v0.4.20 → 本仓库 Release 资产 tag `android-v0.4.20`；安装页 Android 栏目改直链 + sha256。
- (2026-09-09) 制定 Bolloon 安装/下载页设计计划 DRAFT。
- (2026-09-09) 实现并发布安装/下载落地页到 GitHub Pages (logos-42/bolloon-UI)。
- (2026-09-09) 站点扩为多页：产品/安装/文档/Hibs/网关 + 中英切换 + 文档侧边栏 + 网关skills生成器。

## 进行中的实验

- 5 页静态站（index/install/docs/hibs/gateway），炭黑 #1a1a18 + lime #c4d640，编辑式展览排版。
- 顶部导航均布 + 悬浮下拉子菜单；版本号随 npm 包最新版（registry.npmjs.org/@bolloon/bolloon-agent/latest）。
- 文档页：Hermes 风格左侧边栏（16*9 分区）+ 中/EN 语言切换（data-zh/data-en + applyLang + localStorage 记忆）。
- 安装页：栏目标签框（一键脚本/npm/Android/iOS），安卓悬浮子栏已移除。Android = **同域镜像直链** `https://bolloon.cn/dl/bolloon-0.4.22.3.apk`（18.30 MiB / sha256 `55aacaa238665131…` / 正式签名 CN=Bolloon / v0.4.22.3 —— 本版新增左上角「索引」(最近历史会话)、右上角刷新改为「搜索」(本机智能体+好友+全局智能体)、设置加「本机数据」行并删重复的「网络与同步」、网络页「触控控制」纠正为「智能体控制 (MCP / Skills)」、修 Service Worker 陈旧缓存(升级后仍跑旧 JS)；上一版新增底部「好友」tab（连接好友/附近设备/扫码 + P2P 连接状态 + 好友列表，网络页不再重复）、左右滑切 tab / 右滑返回上一层 / 点空白关弹窗、API 配置供应商 6→14 且改成芯片式点选；备用链接指向 GitHub Release tag `android-v0.4.22.2-signed`；页面还写明华为侧载风控（判风险/诈骗）时的处理（退纯净模式 / adb install）。同域镜像来自 `dl/`（gitignore，部署时由 `scripts/deploy-pages.py` 一并上传）。iOS = 三方式 + 未签名 ipa 资产。
- 网关页 gateway.html（agent 专用）：接入说明 + 端点表（/api/agent 五端点 + manifest_request/payload 帧）+ ** skills 生成器**（粘贴 capabilities → 生成可独立打开的纯 HTML 声明页，含 manifest + 入网/委派协议）。
- fig 按颜色错开：产品=219橙风+950品红家、安装=640黑、文档=157金赭、Hibs=375墨绿。
- 部署：Cloudflare Pages（bolloon project，bolloon.pages.dev + 自定义域 bolloon.cn，zone id 9be73c239b5159d75f0e8c62d8b5f41a）与 GitHub Pages（logos-42.github.io/bolloon-UI，source=main//，push 即构建）。**关键**：CF 的 bolloon 项目是 direct-upload 型（`source: null`，历史部署均为 ad_hoc），**GitHub push 不会触发 CF 构建** —— 每次改站必须显式部署，再 purge bolloon.cn 缓存。现统一走 `python3 scripts/deploy-pages.py`：它把仓库根镜像到 `build-site/`（排除 .git/.github/build-site/dl 等），再把 `dl/*.apk` 拷进 `build-site/dl/`（单文件硬上限 25 MiB，超限直接拒），然后用 wrangler 部署 —— **APK 因此从不进 git，却与站点同域同 CDN**（`https://bolloon.cn/dl/<file>`）。`build-site/` 与 `dl/` 均已 gitignore。Release 资产（GitHub）作为备用/校验来源保留。缓存破坏 style.css/app.js?v=N 已升 v=25。
- 本地开发：`python3 -m http.server 8897`；验证用 headless Chrome（dump-dom + 截图）。
