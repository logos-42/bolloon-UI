# Wiki 日志

| 日期 | 事件 | 内容 |
|------|------|------|
| 2026-09-22 | 链上活动区**去假标签** + 口径行上线（v=21，本机 226/0 · 真域名 226/0） | ① **删掉"现在是假的"文案**：`gateway.html` / `index.html` 共 4 处 `data-zh="公开观察入口尚未接入"`（unavailable 态标签 ×2 + unavailable 提示首句 ×2）+ index 顶部注释 1 处 → 改成真话：unavailable 态标签「快照暂时读不到」、提示首句「快照这次没读到 — 本站的数据源是签名快照 network-pulse.json」。**为什么是假的**：公开观察入口早已接入并在真供给数据（同源 `network-pulse.json` 里 25 行 `chain-index` 活动行），说"尚未接入"与事实相反。② **新增口径行**（钩子 `data-pulse-activity-totals`，网关页表格下方 + 首页序栏）+ `app.js` `renderActivityTotals()`：数据全部取自快照 `activity_totals` / `chain_id_scope` / `totals_scope` —— 行数+不同任务（与表格**同源**，前端不自己数行数）、这批行属于哪条链（本机 31337 = 本机隔离开发链 · **不是公网活动**）、以及「上方 任务/已完成/已验证/签名（24h 脉冲事件口径）」与「链上索引 N 行」为何数字不同；老快照缺这三块 → **整行隐藏**（不猜、不编网络名）。③ **缓存破坏 v=20 → v=21**（6 页 12 处）+ `style.css` 两句新样式。④ **`scripts/verify-site.mjs` 防复发**：新增 [6d′]（0 任务 + 25 行同屏必须有口径行解释 · 老快照整行隐藏）、[6e]（同源**真快照**当场验：25 行 + 口径行 + 链归属 + 反矛盾总断言）、[11b]（7 页原始 HTML + gateway/index 渲染后可见文本 + 导航都不得出现"尚未接入"类虚假文案；unavailable 态文案必须是真话）；原有 5 处 `尚未接入` 断言同步改名。⑤ **验收（真跑）**：本机 `python3 -m http.server 8897` + `node scripts/verify-site.mjs http://127.0.0.1:8897` → **226 passed / 0 failed / EXIT=0**；`python3 scripts/deploy-pages.py` → CF Pages **`f035d62f.bolloon.pages.dev`**；`node scripts/verify-site.mjs https://bolloon.cn` → **226 passed / 0 failed / EXIT=0**（含 [6e] 线上真快照那 5 项）。⑥ **线上页面真文本**（headless Chrome dump-dom `https://bolloon.cn/gateway.html`）：表格 25 行 · 口径行逐字「25 行 / 12 个不同任务 · 网络：31337（本机隔离开发链） · 不是公网活动 · 上方 任务/已完成/已验证/签名 只数 24h 窗口内的脉冲事件，链上索引的 25 行（全量，不是 24h 窗口）是另一套口径 —— 不是数据丢了」· 「尚未接入」0 次 / 新文案 2 次；`curl https://bolloon.cn/network-pulse.json` 已带 `activity_totals`(rows 25 / tasks 12 / confirmed 4 / finalized 21) + `chain_id_scope`(31337 · 非公网 · 公网 0 行) + `totals_scope`。**未做**：GitHub Pages 通道需 push 才构建（按约定本次未 push）；快照内部口径收口在 bolloon 仓（见其 `docs/wiki/network-pulse.md` §2.2）。 |
| 2026-09-16 | 隐私政策页上线 bolloon.cn + 部署脚本排掉 .kilo | ① 部署前干跑 `deploy-pages.py --no-deploy` 并扫 `build-site/`：确认无 `.env`/`*.jks`/`*.properties`/secret 类文件，`privacy.html`（19,814 B）、新版 `style.css`、`scripts/*` 与 `dl/bolloon-0.4.22.3.apk`（18.30 MiB，<25 MiB 上限）都在。② 真部署 → **`390e5d0e.bolloon.pages.dev`**（"Uploaded 17 files (129 already uploaded)"，2.4 s）。③ **自定义域立刻拿到新版**：`https://bolloon.cn/privacy.html` → 200 / 20,047 B / `<title>隐私政策 — bolloon</title>`，响应头 `cache-control: public, max-age=0, must-revalidate`（HTML 每次回源校验）→ **无需 purge**；`style.css?v=15` 本身是新 URL，天然绕开缓存。④ **线上验收**：`scripts/verify-privacy.mjs https://bolloon.cn` **38/38**（首跑 37/38 有一项瞬时失败，重跑全绿，无 ❌）；`scripts/verify-site.mjs https://bolloon.cn` **23/23**（站点旧功能无退化）；5 页页脚 privacy 链接 + 备案锚点均在线上；APK 同域镜像 200 / `content-length: 19193025` / `application/vnd.android.package-archive` 未受影响。⑤ **顺带修 `deploy-pages.py`**：`EXCLUDE_DIRS` 加 `.kilo` —— 那是本地 git worktree 的副本目录（6.1 MB 整站重复），旧部署实测 `https://bolloon.cn/.kilo/` = 200 **一直公开着**；本次起不再上传，已验证 `.kilo/worktrees/complete-cicada/docs/wiki/log.md` 现在回的是首页兜底（即该目录已不在部署内）。**未做**：GitHub Pages 通道（source=main// 需 push 才构建，本次按约定未 push）；zone 缓存 purge（当前 wrangler OAuth token 权限只到 `zone (read)`，无 Cache Purge —— 实测不需要，若日后出现陈旧边缘内容需用带权限的 token 或 Dashboard）。 |
| 2026-09-16 | 隐私政策页 + 页脚合规 + v=15 | 新增 `privacy.html`（应用市场表单要填的公开链接）与生成器 `scripts/gen-privacy-page.py`（单一来源可复跑：改文案只改生成器 → 重跑，避免页面/App 内摘要/商店表单三处漂移）。内容：七章 + 三张表（数据清单含"是否离开设备/存放位置/用途/保存期限"、权限逐条、第三方服务含是否出境），中英双语走站点既有 `data-zh/data-en` + `applyLang`。**事实对着代码写**：无账号体系（身份是本机 DID）· 无广告/统计/崩溃 SDK（依赖与源码双向核过）· 蓝牙 `neverForLocation` · 位置 `maxSdkVersion=30` · 相机由系统相机完成不申请 CAMERA · 无障碍与 Shizuku 仅官网直装版 · 境外服务（Cloudflare/GitHub/npm registry/Google Fonts）如实披露 · 注销即时生效 + 异常最长 7 个工作日 + 链上记录不可删。**页脚**：index/install/docs/gateway/hibs 五页统一加「隐私政策」链接（双语）+ **备案号占位注释块**（备案完成后展开注释即公示，链接 beian.miit.gov.cn）；`style.css` 加 `.doc-h2/.doc-list/.doc-table-wrap/.foot-link` 等政策页样式，全站缓存破坏 **v=14 → v=15**。**验收**：新增 `scripts/verify-privacy.mjs`（零依赖、自带 CDP 驱动真 Chrome）本地 **38/38** —— 14 个必填要素 + ≥3 张表 + `data-zh`/`data-en` 数量一致 + 切 EN 后正文真变英文且 `html lang=en` + 5 页页脚链接与备案锚点 + 站内 7 个 html 链接全可达 + 政策页无 JS 报错；站点回归 `scripts/verify-site.mjs` **23/23**（v=15 与页脚改动无退化）。 |
| 2026-09-16 | 商店图标四件套 + 包名迁移（bolloon 仓） | ① **图标**: 从品牌 master `icon.png`(1254×1254, 满幅无圆角无透明) 用 LANCZOS 下采样出 `icons/icon-1024x1024.png`(607.4 KB, md5 `779dd53b8e54f83710cceaa029fa5475`)、`icons/icon-1024x1024.webp`(15.9 KB)、`icons/icon-216x216.png`(30.6 KB)、`icons/icon-216x216.webp`(2.6 KB)，逐条对齐商店规格：正方形 · 216 或 1024 · PNG ≤3 MB · WEBP ≤100 KB · RGB 无 alpha 满幅（圆角由商店自遮罩）；216 档经肉眼复核清晰无锯齿糊边。与 bolloon 仓 `src/web/icons/` **双目录镜像**（该目录原有 7 个 favicon 与原仓逐文件 sha256 一致，本次维持该镜像约定）。② **包名迁移（发生在 bolloon 仓）**: `com.bolloon.agent[.rokid]` → **`com.hibs.bolloon`**（按品牌名定稿），Android/iOS/Capacitor/PWA 标识一次收敛，详见 bolloon 仓 `docs/wiki/log.md` 2026-09-16。**本仓由此产生的待办**: `ios/manifest.plist:43` 的 `bundle-identifier` 仍是 `com.bolloon.agent` —— 与它当前指向的旧 IPA 自洽（不会立刻坏），但**必须与下一个新包名 IPA 一起重出**，否则 OTA 安装清单与 IPA 不一致。**顺带发现真实不一致**: `ios/App/App/Assets.xcassets/AppIcon.appiconset/AppIcon-512@2x.png`(1024×1024) 与品牌 master 不同源（sha256 `a3d1d11e…` vs `02098983…`）→ 三端图标同源待办。 | `icons/icon-1024x1024.png` · `icons/icon-1024x1024.webp` · `icons/icon-216x216.png` · `icons/icon-216x216.webp` · `ios/manifest.plist` |
| 2026-09-15 | 入网 skill 文档 v1.2.1 + iOS 0.4.24 | `bolloon-gateway-join.md` v1.2.0 → **v1.2.1**（+ `skill.html` 同步）：新增 §0.1 **路径 A′ 手机端已内置**（手机本机内核真读说明/本机 DID/服务登记[桌面可达即进网络 registry]/P2P 公告[无对端如实标注]/落盘 `bolloon_gateway_join`），标题改「三条执行路径」；§6 被委派语义回写为**真执行**（严格能力匹配不兜底 `local.agents[0]`、`resultCid` 真 CID 非 `mock-`、超时 504 不假成功）。安装页 iOS 入口 → Release `ios-v0.4.24-unsigned`（`Bolloon-unsigned.ipa` 10,090,692 B / 9.6 MB，`CFBundleShortVersionString=0.4.24`，内含手机端自足入网代码；bolloon 仓 `npm run ios:sim` 真机(模拟器)点「一键入网」实测通过 + 桌面 registry 侧查到该 DID）。缓存破坏 v=13 → **v=14**；`scripts/verify-site.mjs` 断言同步（1.2.1 + 线上正文含「路径 A′」）。 |
| 2026-09-09 | 启动 | 初始化知识系统：建立 wiki、manifest、检查脚本和 repo 级默认规则。 |
| 2026-09-09 | Bolloon 安装/下载页 | 设计计划 DRAFT：读 design.md(数字展馆 brief)+核实 Bolloon 品牌色(style.css)+README 安装命令+package v0.4.20；识别 fig/ 8 图为毛笔书法作品。定稿：纸+炭黑为骨/Bolloon lime 唯一强调色；书法作主视觉展品与能力对位；纯静态 index.html 零构建。计划落 docs/bolloon-install-page-plan.md，未实现 UI。 |
| 2026-09-09 | 安装/下载页发布 | 实现静态落地页 index.html/style.css/app.js(炭黑+lime 编辑式排版、OS 探测、复制按钮、live 版本号)。经本地服务 + headless Chrome(dump-dom+截图)验证渲染/交互。GitHub repo logos-42/bolloon-UI(public, main)，Pages 已启用并构建成功：https://logos-42.github.io/bolloon-UI/ 。注意：git add 曾误裹入全项目，已重建为只含网站文件的干净 commit。 |
| 2026-09-09 | 知识系统初始化 | 用「维基 llm」v2 bootstrap 出 compile-first 知识系统(39 文件)：docs/wiki/（9 页 v2 frontmatter）、manifests/、scripts/（v1+v2 全套校验工具）、平台配置（AGENTS/CLAUDE/.cursorrules/.windsurfrules）、slash 命令、CI workflow；初始化 raw root(bolloon_ui_raw)。修复模板/渲染 4 处缺陷：audience `me`→`self`、runtime-profile 补 v2 必填字段、index/log 与默认规则对齐、渲染器残留哨兵崩溃。校验全绿：wiki_check / wiki_lint --strict=v2 / raw_manifest_check / provenance_check --ci / skill_doctor 均 OK。 |
| 2026-09-09 | 站点扩为多页 + 网关 | 从单安装页扩为 5 页静态站（产品/安装/文档/Hibs/网关）+ figs。关键改动：① 顶部导航改均布+悬浮下拉子菜单（Hibs/产品/安装/文档/网关/GitHub）；② 文档页左侧 Hermes 风格侧边栏（9 分区）+ 中/EN 语言切换(data-zh/data-en+applyLang+localStorage)；③ 安装区合并为一个栏目标签框（一键脚本/npm/Android），去掉安卓悬浮子栏；④ 版本号改为随 npm 包最新版(registry.npmjs.org/@bolloon/bolloon-agent/latest, CORS*)；⑤ 新增 gateway.html（agent 专用）——接入说明+端点表(agent-delegate-server /api/agent 五端点 + manifest_request/payload 帧)+** skills 生成器**（粘贴 capabilities→生成可独立打开的纯 HTML 声明页，含 manifest+入网/委派协议）；⑥ fig 按颜色错开不跨页撞(产品=219橙风+950品红家/安装=640黑/文档=157金赭/Hibs=375墨绿)；⑦ 缓存破坏 style.css/app.js?v=N 逐步升到 v=6，防旧缓存；⑧ 部署 CF Pages(bolloon project)+GitHub Pages+Cloudflare purge。修复：app.js applyOS 空值守卫(非安装页报错)、hibs style.css 缺 v= 导致旧缓存、document P2P frames。 |
| 2026-09-10 | Android APK 发布会话 | 把 bolloon 的 Android debug 包（bolloon-0.4.20.apk，21,038,293 B=20.06 MiB，versionCode 20 / versionName 0.4.20，sha256 7985e67588adfeabc212fcc0cb3830759a1f458272f8683d652a155606402546，由 bolloon master 689c8fe 产出）发布为本仓库 Release 资产 tag=android-v0.4.20（上传后回下载复算 sha256 一致）。install.html 的 Android 栏目从「指向 logos-42/bolloon/releases 的泛链接」改为直链 APK（20.1 MB）+ 短 sha256 + 发行说明链接。双通道部署：GitHub Pages（push main 自动构建，source=main//）+ Cloudflare Pages（bolloon 项目是 **direct-upload 型**(source=null)，必须 `wrangler pages deploy . --project-name=bolloon --branch=main` 从仓库根上传，GitHub push 不会触发它）+ 缓存 purge（zone bolloon.cn 9be73c239b5159d75f0e8c62d8b5f41a）。坑与结论：① 该账号**未启用 R2**（API 10042 "Please enable R2 through the Cloudflare Dashboard"），用户给的 R2 S3 凭证暂时用不了；② 本机无 gh CLI，已装 gh v2.100.0 到 ~/bin；GCM 里 logos-42 的 OAuth token 缺 read:org 被 `gh auth login` 拒绝，改为直接调 REST API + uploads.github.com 建 release；③ CF Pages 单文件上限 25 MiB → 20.06 MiB 的 APK 不进仓库，走 Release 资产（与 iOS .ipa 一致）。 |
| 2026-09-10 | Android 正式签名 APK | 用户手机装 debug 包点开无反应（后确认非 GitHub 托管问题：回下载 sha256 一致、zip CRC 全 OK）。按要求在 bolloon 侧建**固定 release keystore**（android/keystore/bolloon-release.jks，RSA4096/PKCS12/30 年，凭证 android/keystore/keystore.properties，两者已被 android/.gitignore 覆盖；keystore 缺失时 assembleRelease 静默回退 unsigned 以免破坏他人构建），app/build.gradle 加 signingConfigs.release；`./gradlew :app:assembleRelease` 出包 → 18,750,037 B（17.88 MiB，比 debug 小因无调试符号），签名 CN=Bolloon（SHA-256 0789146b…），非 debuggable，仅 v2 签名方案（minSdk 28 足够），assets 与 debug 逐项一致（140 项 / 11,891,814 B / mobile-core.js 3,051,457 B）。发布为 tag=android-v0.4.20-signed（bolloon-0.4.20.apk，sha256 3b5ad96d…，回下载一致），install.html Android 栏目改为指向正式签名版并补「需 Android 9+」提示。**注意签名不兼容**：已装 debug 版需先卸载；两版同 versionCode 20。 |
| 2026-09-13 | 产品页能力精简 + 滚动展示栏 | 按 leo 要求把产品页「它能做什么」从 9 条**精简为最重要的三条**：P2P 网络 / 微支付信息 / 技能沉淀与互传；**序号按顺序重排 01/02/03**（滚动栏与静态列表一致，leo 明确纠正：序号要顺序安排，不用保留原编号 02/06/08）。保留三条的只是**产品页**，安装页/文档页仍为九条。新增**核心能力滚动展示栏** `.cap-marquee`（放在区块标题与静态列表之间）：轨道内含两份等宽内容组、`width:max-content` + `translateX(-50%)` 无缝循环（30s/趟），两侧 `mask-image` 渐隐，`:hover` 暂停，`prefers-reduced-motion: reduce` 下静止且去渐隐遮罩；静态列表排版**保持原样**（编号+标题+说明，仅条目变三条）。缓存破坏 v=12 → **v=13**（style.css 改动，5 页同步）。验证：真 Chrome CDP（bolloon 仓 BrowserCdpSession）**4/4** — ① 静态列表 n=3 且序号 01/02/03、② 两份轨道 + 动画挂载 + overflow:hidden、③ 1s 内 transform 由 -58.11 → -85.35（真在向左流动，≈27px/s 与 817px/30s 吻合）、④ 两份宽 817.19 = 817.19、轨道 1634.38 = 两份之和（-50% 才无缝）；截图人工看图确认上下发丝线与留白正常；**线上** bolloon.cn 复验（静态条目 3、轨道 1s 内 -81.72 → -109.41 仍在流、截图确认）。 |  |
| 2026-09-13 | v0.4.21 内容更新 + 一键入网默认 prompt | 站点内容对齐 npm 最新版 **0.4.21**（registry latest 已从 0.4.20 升上，页头版本徽章自动跟随）。改动：① 产品页/安装页「它能做什么」5 条 → **9 条**（04 工具调用改写为 shell/文件/精确补丁/代码执行/真浏览器/桌面操作与 git；新增 06 微支付信息 x402、07 人机问答 clarify、08 技能沉淀与互传 bolloon://skill/&lt;cid&gt;、09 勿扰时钟），文档页功能区块同步 9 条（中英双语 data-zh/data-en）；② 文档页「使用」加 3 个命令板：`bolloon setup`（配置向导，首次运行也会自动进入）/ `bolloon model`（切换供应商）/ `bolloon x402 list`（微支付信息浏览验真）；③ 安装页「启动」加 `bolloon setup` 与 `bolloon model` 两板；④ 文档页「集成」补一句密钥落盘 `~/.bolloon/llm-config.json`（不入仓库）+ 首次运行进向导；⑤ 参考表加 5 行（llm-config.json / bolloon setup / bolloon model [key &lt;provider&gt;] / bolloon x402 list\|show\|buy\|verify / BOLLOON_CRON=0）；⑥ `?v=11 → v=12`（5 页，app.js 新增 copy key setup/model/x402）；⑦ bolloon 侧手机端新增「一键入网 · 全球智能体网络」点按项，默认 prompt = `read https://bolloon.cn/bolloon-gateway-join.md`，点一下即交智能体执行入网。验证：本地 8898 服务 + 真 Chrome CDP 滚到区块截图（index/install/docs 各 9/9 条，编号对齐无重叠）+ Chrome dump-dom 断言 + **线上** bolloon.cn/、/docs、/install 三页新内容与 v=12 均已生效（/bolloon-gateway-join.md 200）。部署：`wrangler pages deploy . --project-name=bolloon --branch=main`（bolloon 项目为 direct-upload，GitHub push 不触发）→ 28f23058.bolloon.pages.dev；GitHub Pages 走 push main 自动构建。缓存破坏升至 **v=12**。 |
| 2026-09-14 | 维基 llm 全量检查维护 | 跑「维基 llm」全套校验：wiki_check / wiki_lint --strict=v2 / raw_manifest_check / untracked_raw_check / provenance_check --ci / supersede_check / wiki_size_report / version_check。唯一红灯 = untracked_raw_check 报 86 个未登记文件（fig/ 80 个 calligraphy 缩略图 .jpg + icons/ 6 个 favicon .png）。这些是**提交进仓库、随站对外提供的静态站点资产**，非待编译成 wiki 的知识 raw 源；且 raw manifest 为空、raw 源本就放仓库外（CI workflow 注释同）。修法：`scripts/untracked_raw_check.py` SKIP_DIRS 增补 `fig`、`icons`（与 .git/node_modules/manifests 同类排除目录），附注释说明依据。修复后整套检查全绿（untracked_raw_check: OK 0 未登记；wiki_size GREEN ~3,867 tok）。无 schema 改动。 |
| 2026-09-14 | Android 0.4.22 APK 发布 | bolloon master 合并到 cf12333（远程 0.4.22 内容恢复）后重建 APK：`npm run build:web` + `npx cap sync android`（assets 里 mobile.js 命中「微信息」4 / 「一键入网」4 / x402 50，mobile-core.js 5.07 MB）+ `./gradlew :app:assembleRelease`。versionCode 22 / versionName 0.4.22（与 package.json 对齐，此前停 20 是漂移）；19,184,821 B = 18.29 MiB；签名 CN=Bolloon（与 0.4.20 签名版同一 keystore → 可覆盖升级）；v2 方案、非 debuggable；sha256 `0c1383779ce1cada5f1f32620ecd794ac38848313e1b1c6add8e6f01037240c9`，GitHub 侧 asset digest 与本地一致。发布 tag `android-v0.4.22-signed`（资产 bolloon-0.4.22.apk），install.html Android 栏目指针/大小/sha 全部更新为 0.4.22。**新发现的交付风险（重要）**：本机 curl 从 GitHub Release 拉取该资产反复失败（exit 56 接收中断 / exit 28 超时），而 GitHub 后台 digest 表明包本身完好 → 手机端「下载不全 / 点开无反应」大概率出在 github.com 这条传输链，而不是包。待定方向：bolloon.cn 同域镜像（18.29 MiB < CF Pages 单文件 25 MiB，可放）或启用 R2 + 自定义域。 |
| 2026-09-14 | 0.4.22.2: 好友 tab + 手势 + 多供应商 | ① **新增底部 tab「好友」**（首页之后、网络之前）：把原来散在网络页的 连接好友 / 附近设备 / 扫码入网·加好友 + P2P 连接状态 + P2P 好友 / 我的 P2P ID + 好友列表 全搬过去，**网络页不再重复**（这就是「去掉重复信息栏」）。HTML 用脚本按 id 搬行（不用 patch 前缀锚点），搬完断言各 id 唯一。② **手势**（`setupGestures`，挂在 document 上，passive）：左右滑切 tab（60px 门槛 + 横向占优 1.4× + 900ms 内 + 横向滚动容器内不抢手势）；**右滑返回上一层**（取 z-index 最高的可见浮层 crop-modal 90 / sheet 80 / chat-page 60 / card-detail 50，按类型走它自己的关闭路径 —— chat-page 点它左上角 ←、sheet 直接 hidden、crop-modal 点取消）；**点弹窗空白处关弹窗**（点 `.sheet` 暗背景且不在 `.sheet-inner` 内即关）。③ **API 配置供应商 6→14**（DeepSeek/OpenAI/Anthropic/Gemini/Grok/通义千问/智谱 GLM/Kimi/MiniMax/硅基流动/Groq/OpenRouter/本地 Ollama/自定义；全部 OpenAI 兼容，因手机端 `RemoteLlm` 只走 `baseUrl + /chat/completions`），**选择栏由原生 select 改为芯片式**（`.provider-chip`，选中染 lime，并显示「已配 N 个」）。**验证（真 headless Chromium 驱动打包产物）**：4 tab 与 4 页一一对应、逐个点击切换标题正确（首页/好友/网络/我）；合成 TouchEvent 左滑→好友、右滑→首页；sheet 内点内容不关/点背景关；带 ← 的浮层右滑 → 按钮被点且浮层移除；provider 常量 14 条、旧 select 0 残留。产物 `bolloon-0.4.22.2.apk` 19,188,513 B = 18.30 MiB，CN=Bolloon，sha256 `6ff6e0a6…`（GitHub digest + 同域镜像回下载双验一致），tag `android-v0.4.22.2-signed`。**部署坑记录**：`deploy-pages.py` 之后自定义域约 30-60s 才切到新部署 —— 期间请求 `/dl/xxx.apk` 会因 SPA 回退返回 **index.html（HTTP 200，8.7KB）**，所以验收必须看 Content-Type/大小/哈希，不能只看状态码。 |
| 2026-09-14 | 0.4.22.1: 扫码开相机 + 触控一键按钮 | 用户报两个真机缺陷：① 右上角「扫码添加」与网络页「扫码入网 / 加好友」打开的是**相册**而不是相机；② 手机端「MCP 工具（触控调用）」点了没反应。**① 根因（确定性，非设备差异）**：Capacitor 8 的库 manifest 是空的 —— App 必须自己声明 FileProvider（authority 必须严格是 `${applicationId}.fileprovider`，因为 `BridgeWebChromeClient.createImageFileUri()` 硬编码它），否则创建拍照 URI 抛异常 → Capacitor 静默回退文件选择器；且 Android 11+ 包可见性过滤下，不声明 `<queries><intent>IMAGE_CAPTURE</intent></queries>` 时 `resolveActivity` 恒为 null → 同样回退相册。修复：补 `res/xml/file_paths.xml`（external-files-path/files/cache）+ FileProvider + queries(IMAGE_CAPTURE / GET_CONTENT)。**②** 那个列表每项调 `window.__mobileTouch`，而它在 mobile.js 里是空实现、全仓无原生注入 → 点了必然没反应；按用户要求**收敛成一个按钮**：网络页「触控控制」→ 原生新增插件方法 `touchStatus`（ready/enabled/hint 三态）+ `openAccessibilitySettings`（跳系统无障碍设置），文案随状态变（开启触控控制 / 未连上 / 已就绪）。**③ 版本策略改为补丁位递增**：versionName `0.4.22.1`（versionCode 23），往后 `0.4.22.2`。产物 `bolloon-0.4.22.1.apk` 19,186,085 B = 18.30 MiB，CN=Bolloon（同 keystore 可覆盖升级），sha256 `98ee32ef…`（GitHub asset digest 与同域镜像回下载**双验一致**）；发布 tag `android-v0.4.22.1-signed`，安装页指针/sha/大小已更新，并补了「华为把自签名+无障碍包判成诈骗/风险时：退纯净模式或用电脑 adb install」的说明。验证：包内 manifest 确认含 `com.bolloon.agent.rokid.fileprovider` + `FILE_PROVIDER_PATHS` + `queries(IMAGE_CAPTURE)`；assets 内「触控控制」命中 1、旧「MCP 工具 (触控调用)」0 残留。 |
| 2026-09-14 | 同域 APK 镜像（修「点开无反应」） | **复现**: 本机 `curl https://github.com` 直接超时（`Failed to connect to github.com port 443 after 21043 ms`，exit 28），GitHub Release 资产反复拉不动（exit 56 接收中断 / exit 28 超时）；而 GitHub 侧 asset digest 与本地逐字节一致 → 包没问题，是 github.com 这条链在国内连不上，手机端「下载了但点开无反应」同源（拿到的是残包/错误页）。**修复**: 站点自己托管一份**同域镜像** —— 新增 `scripts/deploy-pages.py`（把仓库根镜像到 `build-site/`，排除 .git/.github/build-site/dl；再把 `dl/*.apk` 拷进 `build-site/dl/`，超 25 MiB 直接拒；最后 wrangler 部署），`dl/` 与 `build-site/` 进 .gitignore（APK 仍不入 git，却与站点同域同 CDN）；install.html 主按钮改指 `https://bolloon.cn/dl/bolloon-0.4.22.apk`，GitHub Release 降为「备用下载 (GitHub)」；安装说明补「系统会问一次『安装未知应用』，允许你用来下载的那个浏览器」。**验证**: 新部署 487252ca 与 `bolloon.cn/dl/bolloon-0.4.22.apk` 均 200、19,184,821 B、sha256 与本地构建一致（3.0 MB/s；bolloon.cn 路径 178 KB/s 但完整下载），bolloon.cn / www / pages.dev / github.io 四条 install.html 均含 `bolloon.cn/dl` 链接；缓存已 purge。**坑**: Windows 上 `wrangler` 是 .cmd，Python subprocess 直接调会 `FileNotFoundError` → 必须经 `cmd /c`。 |
| 2026-09-14 | 0.4.22.3: 索引/搜索 + MCP·Skills 控制 + SW 修复 | 产物 `bolloon-0.4.22.3.apk` 19,193,025 B / sha256 `55aacaa238665131…` / 正式签名 CN=Bolloon (versionCode 25)，tag `android-v0.4.22.3-signed`；同域镜像 `https://bolloon.cn/dl/bolloon-0.4.22.3.apk`（回下载整包复算 sha256 一致，Content-Type=application/vnd.android.package-archive / Length=19,193,025），CF Pages 部署 `0cd0996e.bolloon.pages.dev` + purge；四域名 install.html 同步 0.4.22.3。手机端本版：左上角**索引**（最近历史会话，读本机 IndexedDB）、右上角刷新→**搜索**（本机智能体+好友+全局智能体）、好友页 P2P ID 移到 P2P 连接之前、设置删「网络与同步」并加「本机数据」行/无障碍项、网络页「触控控制」纠正为**智能体控制 (MCP / Skills)**；另修 **Service Worker 陈旧缓存**（原 cache-first + 固定缓存名 → 升级后 WebView 仍跑旧 JS，改 network-first + 缓存名 v2）。 |
| 2026-09-15 | 入网 skill 文档同步到 bolloon 0.4.23（v1.1.0 → v1.2.0） | bolloon 侧当天修了两个真 bug 并补了「读说明即入网」的工具入口，文档必须同步，否则「读到即执行」会照抄过时伪码：① 新增 **§0.1 两条执行路径** —— 路径 A（本机就是 bolloon）直接调工具 `join_global_gateway`（默认 url = 本文档；读说明 → DID → P2P 节点 → manifest → 可分享 `orbitdb://` 网络 → 服务登记 → `~/.bolloon/gateway-join.json` 幂等落盘），并写明「agent 手里是工具不是 SDK，照抄下面的 TS 伪码反而会失败」；路径 B（第三方运行时）才按 §1–§6 自建节点。② **§7 签名与地址广播** 重写为「首次接触 TOFU」：`AddressBroadcast` 新增 `publicKey`（hex，**且在签名覆盖范围内**）—— 全球网络里收方此前不认识发方，没有它只能丢弃广播（陌生人永远发现不了彼此）；收方顺序 = 时间戳漂移 >24h 拒收 → 已知 DID 用已存公钥验签、公钥不一致**拒收且不覆盖**（身份接管防护）→ 未知 DID 用自携公钥验签（TOFU）+ `did:key` 做 DID↔公钥派生一致性检查（base58btc 解 `0xed01‖32B` 比对，冒充者换公钥即解不出同 DID）→ 通过才写 registry 且存**对方**公钥。③ §3 注明 **bolloon 节点启动即挂载 `/api/agent`**（此前只在 iroh 懒初始化时挂，未触发则恒 404）。④ §9 排错新增 4 行（`/api/agent/*` 404 / unknown + 无 publicKey 拒收 / did:key 与公钥不匹配拒收 / 公钥与已知不一致拒收）。⑤ §10 持久化补 `gateway-join.json`。frontmatter `version: 1.1.0 → 1.2.0`，description 改写为「两种路径」概述；`skill.html` 全量同步（含版本号与 §0.1/§7/§9/§10）。 |
| 2026-09-15 | 版本徽章去硬编码 + CF Pages 部署 + 同域 APK 镜像补回 | leo 指出徽章仍有旧的硬编码残留：**5 页静态 HTML 内联 `<span id="version">0.4.20</span>`**（断网 / JS 失败时页面会谎报过期版本）→ 全部改为占位 `—`；`app.js` 删掉 `VERSION_FALLBACK = '0.4.20'` 常量与「先写死再覆盖」逻辑，改为只认真实数据（npm registry latest，自带 CORS），失败回退 GitHub release tag，**且只在 tag 形如 `0.4.23` 时才采用**（`android-v0.4.22.3-signed` 这类 tag 一律忽略），两者都取不到就保持 `—`。**部署前发现 `dl/` 被清空** —— 直接跑 `deploy-pages.py` 会把同域 APK 镜像从站点抹掉（install.html 主按钮指向 `https://bolloon.cn/dl/bolloon-0.4.22.3.apk`），故先从 GitHub Release 拉回 0.4.22.3 资产（19,193,025 B，sha256 `55aacaa238665131…` 与记录一致）放回 `dl/` 再部署。部署 `python3 scripts/deploy-pages.py` → CF Pages `1de7518a.bolloon.pages.dev`（14 文件新上传，含 dl/ 镜像）。**新增可复用验收脚本 `scripts/verify-site.mjs`**（零依赖：node 全局 WebSocket 直驱 headless Chrome 的 CDP），覆盖 ① 5 页徽章 = live npm 版本 ② 静态 HTML 占位非版本号 ③ skill.html 已同步 v1.2.0 ④ gateway.html 粘贴命令跟随访问源 ⑤ `/bolloon-gateway-join.md` 正文 v1.2.0。真跑：本地 8898 **21/21**、线上 bolloon.cn **21/21**；另 `curl -r 0-1` 复核镜像 `HTTP 206 / content-type application/vnd.android.package-archive / content-range 0-1/19193025`。 |
| 2026-09-19 | Android 0.4.28 发布 (包名迁移后首个 APK) | 产物 `bolloon-0.4.28.apk` 19,915,587 B / sha256 `d339065a6d6fe397…` / 正式签名 CN=Bolloon (证书 SHA-256 `0789146b…`) / versionCode 28 · versionName 0.4.28 / 包名 **com.hibs.bolloon**（品牌迁移后新包名，与旧 0.4.22.x 不同应用，需先卸载旧版）/ 官网直装版 **full flavor**（`./gradlew :app:assembleFullRelease`，保留无障碍服务 + Shizuku；商店版 store flavor 由 src/store 清单摘除）；tag `android-v0.4.28-signed`（asset digest 与本地一致）；同域镜像 `https://bolloon.cn/dl/bolloon-0.4.28.apk`（Content-Type=application/vnd.android.package-archive / Length=19,915,587）；CF Pages 部署 `46f89480.bolloon.pages.dev`；四域名 install.html 同步 0.4.28（iOS 入口仍为 `ios-v0.4.24-unsigned` 未动）。流程: `npm run build:web` → `npx cap sync android` → `./gradlew :app:assembleFullRelease` → 验签/验版本/验包内 UI → Release + 镜像 + 安装页 + 部署。踩坑: 首建 release 时误用树里的旧 versionCode 26/0.4.24，用户纠正「最新版是 0.4.28」→ 删掉误建 release+tag (`android-v0.4.24-signed`) 后按 0.4.28 重建。 |

## [2026-09-22] feat(layout) | 网关两栏(链上活动左·加入方式右) + 首页恢复「加入网络」按钮(开始安装右侧)

**leo 原话**: 「bolloon-UI, 把网关里面的**加入方式位置放在链上活动右侧**; 首页里面的**加入网络按钮需要有, 放在开始安装右侧**」。

- **网关页 `gateway.html`**: `#pulse`(① 链上活动 · 仪表盘) 与 `#skills`(② 加入方式 · 一条命令) 包进 `<div class="gateway-row">`; 编号顺读序改为 01 链上活动(左) → 02 加入方式(右) → 03 如何加入。
- **首页 `index.html`**: 新增 `<a class="quiet-cta ghost join-network-cta" href="gateway.html" data-zh="加入网络" data-en="Join the network">`, 位于「开始安装」**右侧**的同一 CTA 行。
- **`style.css`**: `.gateway-row{display:grid; grid-template-columns:minmax(0,1.72fr) minmax(0,1fr)}`(**`minmax(0,…)` 不能省** —— grid 子项默认 `min-width:auto`, 长表格会把列撑爆横向溢出); 子 section 容器样式**清零**(否则「容器套容器」双重缩进且两栏各占满 1240px 无法并排); ≤900px 堆叠; `.intro-cta{flex-wrap:wrap}`(390px 不溢出); CTA/导航/语言切换加 `:focus-visible` lime 焦点环(键盘可达, 鼠标点击不亮)。
- **缓存破坏**: `v=21 → v=22`(6 页同步)。
- **验收**: `scripts/verify-site.mjs` 226 → **246 项**, 新增含**真布局测量**(桌面 1440px 两栏 sameRow 且右者 x 更大 + 两栏间有真实横向间隔防「负 margin 假并排」; 390px 堆叠且不溢出; 首页按钮存在且在「开始安装」右侧; href 指向网关页)。
- **钥匙**: 上一轮为删「加入网络」序厅而定的断言 [11] 只允许该中文短语在**首页这个新按钮**出现(已注明它防的是「与事实相反的虚假文案」, 不是防按钮本身)。
- **我(父 agent)接手后的复核**: 本机 `node scripts/verify-site.mjs http://127.0.0.1:8921` → **246 passed / 0 failed**。
- **踩坑(已修)**: 子智能体原用「`Page.navigate` → 固定 `sleep(1500)` → 探针」量快照档位, 会读到**中间态**(实测 `rows:0` 的假失败) → 改为 `waitStable(probe, v => v.state==='live' && v.act.rowCount===25)`;**同一条原则也应用到了新加的布局断言**, 否则将来会以同样方式抖成假红 —— 这正是 leo 明确不要的「会撒谎的门」。

## [2026-09-22] fix(sync) | SKILL 镜像同步 v1.2.0 + 修「固定 sleep 量到中间态」的假红 (我复跑 246/0 本机+真域名)

- **镜像同步**: 主仓 `skills/bolloon-network/SKILL.md` **v1.1.0 → v1.2.0**(P6b 新增 3 个链上**写** tool + 授权意图参数) → 本仓 `bolloon-network.md` 复制同步 · `skill.html` 索引 `1.1.0 → 1.2.0` · `scripts/verify-site.mjs` **7 处逐字断言**一并改 ⇒ 本仓 `1.1.0` 残留 **0**。
- **修真缺陷(会撒谎的门)**: 新加断言里有几处是「`Page.navigate` → 固定 `sleep(900)` → 探针」; 线上 CDN 更慢 ⇒ 量到**文档还没画完的中间态**: 真域名实测 `首页脉冲区内部节点一律用 data-pulse-* 钩子 — {"ids":[],"roots":0}`。更危险的是同类的「**缺失类**」断言: `无重复 id` 在空文档下 `[id]` 为空 ⇒ **不可能重复 ⇒ 假绿**。修法: 改 `waitUntil(document.readyState==='complete' && 目标节点已出现)` —— **断言一字未动, 只加等待**。
- **对照组(旧代码)**: 真域名两遍共 **5 项红**(roots:0 · 切回中文 · 相对时间 tick · prefers-reduced-motion · 390px 堆叠, 值全呈"未渲染"形态) → 修后我复核: 本机 **246/0** · 真域名 **246/0**。
- **如实残留**: 该脚本仍有 8 处固定 `sleep(9xx/1xxx)`(411/442/1286/1292/1591/1625/1712/1767), 同类脆弱点; 本轮只修实测红的两处 + 最危险的一处, 不顺手全改。

## [2026-09-22] fix(site-verify) | 夹具门改成「不会撒谎」+ 我复跑 4 遍全绿 (本机 246/0 · 真域名两遍 246/0)

- **问题**: 同一条命令在**真域名上时绿时红** —— 一轮 `246 passed/0 failed`,再跑一轮 `238 passed/8 failed`,8 条全在「容错/夹具」节,失败值全是探针没取到东西的形态(`rows:0` / `[]` / `ago:""`)。红的时候**分不清是页面坏了还是夹具没注入** —— 会撒谎的门。
- **修法**: ① **夹具自证** —— 每次 CDP Fetch 注入夹具后、断言之前, 先验证夹具真生效(链路证 marker 回夹具次数 + 消费证 DOM 标记 `__vfy:<mode>:__`), 打印 `🔒 夹具自证 … 已满足`;② 拿不到则明确报「夹具未生效(拦截未命中)」而非笼统 `[]`, 失败消息一眼可区分「页面错 vs 夹具错」;③ 断言只加不减;④ 结果行改 `=== 结果: N passed, M failed, K skipped ===`(skipped 透明可见, 不许默默跳过)。
- **我独立复跑(4 遍)**: 本机 `246 passed / 0 failed / 0 skipped`(EXIT=0) · 真域名第 1 遍 `246/0/0` · 第 2 遍 `246/0/0` · 前面那遍干净本机 `246/0/0`。**`skipped = 0` 说明不是靠跳过刷绿**(每条夹具断言都带自证真跑)。
- **未提交**: `scripts/shots.mjs`(截图助手, 子智能体遗留)与空目录 `scripts/fixtures/`(夹具为运行时生成)。
