# Wiki 日志

| 日期 | 事件 | 内容 |
|------|------|------|
| 2026-09-09 | 启动 | 初始化知识系统：建立 wiki、manifest、检查脚本和 repo 级默认规则。 |
| 2026-09-09 | Bolloon 安装/下载页 | 设计计划 DRAFT：读 design.md(数字展馆 brief)+核实 Bolloon 品牌色(style.css)+README 安装命令+package v0.4.20；识别 fig/ 8 图为毛笔书法作品。定稿：纸+炭黑为骨/Bolloon lime 唯一强调色；书法作主视觉展品与能力对位；纯静态 index.html 零构建。计划落 docs/bolloon-install-page-plan.md，未实现 UI。 |
| 2026-09-09 | 安装/下载页发布 | 实现静态落地页 index.html/style.css/app.js(炭黑+lime 编辑式排版、OS 探测、复制按钮、live 版本号)。经本地服务 + headless Chrome(dump-dom+截图)验证渲染/交互。GitHub repo logos-42/bolloon-UI(public, main)，Pages 已启用并构建成功：https://logos-42.github.io/bolloon-UI/ 。注意：git add 曾误裹入全项目，已重建为只含网站文件的干净 commit。 |
| 2026-09-09 | 知识系统初始化 | 用「维基 llm」v2 bootstrap 出 compile-first 知识系统(39 文件)：docs/wiki/（9 页 v2 frontmatter）、manifests/、scripts/（v1+v2 全套校验工具）、平台配置（AGENTS/CLAUDE/.cursorrules/.windsurfrules）、slash 命令、CI workflow；初始化 raw root(bolloon_ui_raw)。修复模板/渲染 4 处缺陷：audience `me`→`self`、runtime-profile 补 v2 必填字段、index/log 与默认规则对齐、渲染器残留哨兵崩溃。校验全绿：wiki_check / wiki_lint --strict=v2 / raw_manifest_check / provenance_check --ci / skill_doctor 均 OK。 |
| 2026-09-09 | 站点扩为多页 + 网关 | 从单安装页扩为 5 页静态站（产品/安装/文档/Hibs/网关）+ figs。关键改动：① 顶部导航改均布+悬浮下拉子菜单（Hibs/产品/安装/文档/网关/GitHub）；② 文档页左侧 Hermes 风格侧边栏（9 分区）+ 中/EN 语言切换(data-zh/data-en+applyLang+localStorage)；③ 安装区合并为一个栏目标签框（一键脚本/npm/Android），去掉安卓悬浮子栏；④ 版本号改为随 npm 包最新版(registry.npmjs.org/@bolloon/bolloon-agent/latest, CORS*)；⑤ 新增 gateway.html（agent 专用）——接入说明+端点表(agent-delegate-server /api/agent 五端点 + manifest_request/payload 帧)+** skills 生成器**（粘贴 capabilities→生成可独立打开的纯 HTML 声明页，含 manifest+入网/委派协议）；⑥ fig 按颜色错开不跨页撞(产品=219橙风+950品红家/安装=640黑/文档=157金赭/Hibs=375墨绿)；⑦ 缓存破坏 style.css/app.js?v=N 逐步升到 v=6，防旧缓存；⑧ 部署 CF Pages(bolloon project)+GitHub Pages+Cloudflare purge。修复：app.js applyOS 空值守卫(非安装页报错)、hibs style.css 缺 v= 导致旧缓存、document P2P frames。 |
| 2026-09-10 | Android APK 发布会话 | 把 bolloon 的 Android debug 包（bolloon-0.4.20.apk，21,038,293 B=20.06 MiB，versionCode 20 / versionName 0.4.20，sha256 7985e67588adfeabc212fcc0cb3830759a1f458272f8683d652a155606402546，由 bolloon master 689c8fe 产出）发布为本仓库 Release 资产 tag=android-v0.4.20（上传后回下载复算 sha256 一致）。install.html 的 Android 栏目从「指向 logos-42/bolloon/releases 的泛链接」改为直链 APK（20.1 MB）+ 短 sha256 + 发行说明链接。双通道部署：GitHub Pages（push main 自动构建，source=main//）+ Cloudflare Pages（bolloon 项目是 **direct-upload 型**(source=null)，必须 `wrangler pages deploy . --project-name=bolloon --branch=main` 从仓库根上传，GitHub push 不会触发它）+ 缓存 purge（zone bolloon.cn 9be73c239b5159d75f0e8c62d8b5f41a）。坑与结论：① 该账号**未启用 R2**（API 10042 "Please enable R2 through the Cloudflare Dashboard"），用户给的 R2 S3 凭证暂时用不了；② 本机无 gh CLI，已装 gh v2.100.0 到 ~/bin；GCM 里 logos-42 的 OAuth token 缺 read:org 被 `gh auth login` 拒绝，改为直接调 REST API + uploads.github.com 建 release；③ CF Pages 单文件上限 25 MiB → 20.06 MiB 的 APK 不进仓库，走 Release 资产（与 iOS .ipa 一致）。 |
| 2026-09-10 | Android 正式签名 APK | 用户手机装 debug 包点开无反应（后确认非 GitHub 托管问题：回下载 sha256 一致、zip CRC 全 OK）。按要求在 bolloon 侧建**固定 release keystore**（android/keystore/bolloon-release.jks，RSA4096/PKCS12/30 年，凭证 android/keystore/keystore.properties，两者已被 android/.gitignore 覆盖；keystore 缺失时 assembleRelease 静默回退 unsigned 以免破坏他人构建），app/build.gradle 加 signingConfigs.release；`./gradlew :app:assembleRelease` 出包 → 18,750,037 B（17.88 MiB，比 debug 小因无调试符号），签名 CN=Bolloon（SHA-256 0789146b…），非 debuggable，仅 v2 签名方案（minSdk 28 足够），assets 与 debug 逐项一致（140 项 / 11,891,814 B / mobile-core.js 3,051,457 B）。发布为 tag=android-v0.4.20-signed（bolloon-0.4.20.apk，sha256 3b5ad96d…，回下载一致），install.html Android 栏目改为指向正式签名版并补「需 Android 9+」提示。**注意签名不兼容**：已装 debug 版需先卸载；两版同 versionCode 20。 |
| 2026-09-13 | 产品页能力精简 + 滚动展示栏 | 按 leo 要求把产品页「它能做什么」从 9 条**精简为最重要的三条**：P2P 网络 / 微支付信息 / 技能沉淀与互传；**序号按顺序重排 01/02/03**（滚动栏与静态列表一致，leo 明确纠正：序号要顺序安排，不用保留原编号 02/06/08）。保留三条的只是**产品页**，安装页/文档页仍为九条。新增**核心能力滚动展示栏** `.cap-marquee`（放在区块标题与静态列表之间）：轨道内含两份等宽内容组、`width:max-content` + `translateX(-50%)` 无缝循环（30s/趟），两侧 `mask-image` 渐隐，`:hover` 暂停，`prefers-reduced-motion: reduce` 下静止且去渐隐遮罩；静态列表排版**保持原样**（编号+标题+说明，仅条目变三条）。缓存破坏 v=12 → **v=13**（style.css 改动，5 页同步）。验证：真 Chrome CDP（bolloon 仓 BrowserCdpSession）**4/4** — ① 静态列表 n=3 且序号 01/02/03、② 两份轨道 + 动画挂载 + overflow:hidden、③ 1s 内 transform 由 -58.11 → -85.35（真在向左流动，≈27px/s 与 817px/30s 吻合）、④ 两份宽 817.19 = 817.19、轨道 1634.38 = 两份之和（-50% 才无缝）；截图人工看图确认上下发丝线与留白正常；**线上** bolloon.cn 复验（静态条目 3、轨道 1s 内 -81.72 → -109.41 仍在流、截图确认）。 |  |
| 2026-09-13 | v0.4.21 内容更新 + 一键入网默认 prompt | 站点内容对齐 npm 最新版 **0.4.21**（registry latest 已从 0.4.20 升上，页头版本徽章自动跟随）。改动：① 产品页/安装页「它能做什么」5 条 → **9 条**（04 工具调用改写为 shell/文件/精确补丁/代码执行/真浏览器/桌面操作与 git；新增 06 微支付信息 x402、07 人机问答 clarify、08 技能沉淀与互传 bolloon://skill/&lt;cid&gt;、09 勿扰时钟），文档页功能区块同步 9 条（中英双语 data-zh/data-en）；② 文档页「使用」加 3 个命令板：`bolloon setup`（配置向导，首次运行也会自动进入）/ `bolloon model`（切换供应商）/ `bolloon x402 list`（微支付信息浏览验真）；③ 安装页「启动」加 `bolloon setup` 与 `bolloon model` 两板；④ 文档页「集成」补一句密钥落盘 `~/.bolloon/llm-config.json`（不入仓库）+ 首次运行进向导；⑤ 参考表加 5 行（llm-config.json / bolloon setup / bolloon model [key &lt;provider&gt;] / bolloon x402 list\|show\|buy\|verify / BOLLOON_CRON=0）；⑥ `?v=11 → v=12`（5 页，app.js 新增 copy key setup/model/x402）；⑦ bolloon 侧手机端新增「一键入网 · 全球智能体网络」点按项，默认 prompt = `read https://bolloon.cn/bolloon-gateway-join.md`，点一下即交智能体执行入网。验证：本地 8898 服务 + 真 Chrome CDP 滚到区块截图（index/install/docs 各 9/9 条，编号对齐无重叠）+ Chrome dump-dom 断言 + **线上** bolloon.cn/、/docs、/install 三页新内容与 v=12 均已生效（/bolloon-gateway-join.md 200）。部署：`wrangler pages deploy . --project-name=bolloon --branch=main`（bolloon 项目为 direct-upload，GitHub push 不触发）→ 28f23058.bolloon.pages.dev；GitHub Pages 走 push main 自动构建。缓存破坏升至 **v=12**。 |
| 2026-09-14 | 维基 llm 全量检查维护 | 跑「维基 llm」全套校验：wiki_check / wiki_lint --strict=v2 / raw_manifest_check / untracked_raw_check / provenance_check --ci / supersede_check / wiki_size_report / version_check。唯一红灯 = untracked_raw_check 报 86 个未登记文件（fig/ 80 个 calligraphy 缩略图 .jpg + icons/ 6 个 favicon .png）。这些是**提交进仓库、随站对外提供的静态站点资产**，非待编译成 wiki 的知识 raw 源；且 raw manifest 为空、raw 源本就放仓库外（CI workflow 注释同）。修法：`scripts/untracked_raw_check.py` SKIP_DIRS 增补 `fig`、`icons`（与 .git/node_modules/manifests 同类排除目录），附注释说明依据。修复后整套检查全绿（untracked_raw_check: OK 0 未登记；wiki_size GREEN ~3,867 tok）。无 schema 改动。 |
| 2026-09-14 | Android 0.4.22 APK 发布 | bolloon master 合并到 cf12333（远程 0.4.22 内容恢复）后重建 APK：`npm run build:web` + `npx cap sync android`（assets 里 mobile.js 命中「微信息」4 / 「一键入网」4 / x402 50，mobile-core.js 5.07 MB）+ `./gradlew :app:assembleRelease`。versionCode 22 / versionName 0.4.22（与 package.json 对齐，此前停 20 是漂移）；19,184,821 B = 18.29 MiB；签名 CN=Bolloon（与 0.4.20 签名版同一 keystore → 可覆盖升级）；v2 方案、非 debuggable；sha256 `0c1383779ce1cada5f1f32620ecd794ac38848313e1b1c6add8e6f01037240c9`，GitHub 侧 asset digest 与本地一致。发布 tag `android-v0.4.22-signed`（资产 bolloon-0.4.22.apk），install.html Android 栏目指针/大小/sha 全部更新为 0.4.22。**新发现的交付风险（重要）**：本机 curl 从 GitHub Release 拉取该资产反复失败（exit 56 接收中断 / exit 28 超时），而 GitHub 后台 digest 表明包本身完好 → 手机端「下载不全 / 点开无反应」大概率出在 github.com 这条传输链，而不是包。待定方向：bolloon.cn 同域镜像（18.29 MiB < CF Pages 单文件 25 MiB，可放）或启用 R2 + 自定义域。 |
| 2026-09-14 | 0.4.22.1: 扫码开相机 + 触控一键按钮 | 用户报两个真机缺陷：① 右上角「扫码添加」与网络页「扫码入网 / 加好友」打开的是**相册**而不是相机；② 手机端「MCP 工具（触控调用）」点了没反应。**① 根因（确定性，非设备差异）**：Capacitor 8 的库 manifest 是空的 —— App 必须自己声明 FileProvider（authority 必须严格是 `${applicationId}.fileprovider`，因为 `BridgeWebChromeClient.createImageFileUri()` 硬编码它），否则创建拍照 URI 抛异常 → Capacitor 静默回退文件选择器；且 Android 11+ 包可见性过滤下，不声明 `<queries><intent>IMAGE_CAPTURE</intent></queries>` 时 `resolveActivity` 恒为 null → 同样回退相册。修复：补 `res/xml/file_paths.xml`（external-files-path/files/cache）+ FileProvider + queries(IMAGE_CAPTURE / GET_CONTENT)。**②** 那个列表每项调 `window.__mobileTouch`，而它在 mobile.js 里是空实现、全仓无原生注入 → 点了必然没反应；按用户要求**收敛成一个按钮**：网络页「触控控制」→ 原生新增插件方法 `touchStatus`（ready/enabled/hint 三态）+ `openAccessibilitySettings`（跳系统无障碍设置），文案随状态变（开启触控控制 / 未连上 / 已就绪）。**③ 版本策略改为补丁位递增**：versionName `0.4.22.1`（versionCode 23），往后 `0.4.22.2`。产物 `bolloon-0.4.22.1.apk` 19,186,085 B = 18.30 MiB，CN=Bolloon（同 keystore 可覆盖升级），sha256 `98ee32ef…`（GitHub asset digest 与同域镜像回下载**双验一致**）；发布 tag `android-v0.4.22.1-signed`，安装页指针/sha/大小已更新，并补了「华为把自签名+无障碍包判成诈骗/风险时：退纯净模式或用电脑 adb install」的说明。验证：包内 manifest 确认含 `com.bolloon.agent.rokid.fileprovider` + `FILE_PROVIDER_PATHS` + `queries(IMAGE_CAPTURE)`；assets 内「触控控制」命中 1、旧「MCP 工具 (触控调用)」0 残留。 |
| 2026-09-14 | 同域 APK 镜像（修「点开无反应」） | **复现**: 本机 `curl https://github.com` 直接超时（`Failed to connect to github.com port 443 after 21043 ms`，exit 28），GitHub Release 资产反复拉不动（exit 56 接收中断 / exit 28 超时）；而 GitHub 侧 asset digest 与本地逐字节一致 → 包没问题，是 github.com 这条链在国内连不上，手机端「下载了但点开无反应」同源（拿到的是残包/错误页）。**修复**: 站点自己托管一份**同域镜像** —— 新增 `scripts/deploy-pages.py`（把仓库根镜像到 `build-site/`，排除 .git/.github/build-site/dl；再把 `dl/*.apk` 拷进 `build-site/dl/`，超 25 MiB 直接拒；最后 wrangler 部署），`dl/` 与 `build-site/` 进 .gitignore（APK 仍不入 git，却与站点同域同 CDN）；install.html 主按钮改指 `https://bolloon.cn/dl/bolloon-0.4.22.apk`，GitHub Release 降为「备用下载 (GitHub)」；安装说明补「系统会问一次『安装未知应用』，允许你用来下载的那个浏览器」。**验证**: 新部署 487252ca 与 `bolloon.cn/dl/bolloon-0.4.22.apk` 均 200、19,184,821 B、sha256 与本地构建一致（3.0 MB/s；bolloon.cn 路径 178 KB/s 但完整下载），bolloon.cn / www / pages.dev / github.io 四条 install.html 均含 `bolloon.cn/dl` 链接；缓存已 purge。**坑**: Windows 上 `wrangler` 是 .cmd，Python subprocess 直接调会 `FileNotFoundError` → 必须经 `cmd /c`。 |
