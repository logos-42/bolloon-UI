.obsidian/
raw/
raw_local/
raw_vault/
.wrangler/

# 部署产物 + 同域 APK 镜像（脚本 scripts/deploy-pages.py 生成/上传, 不进 git）
build-site/
dl/

# 临时备份(bootstrap --force 覆盖时生成)
*.bak.*
*.bak.

# 发布脚本临时产物 (GitHub API 响应)
rel.json
up.json

# 公开观察入口的动态快照 (每次刷新由 scripts/refresh-pulse.sh 重新生成, 不进 git 免噪音)
network-pulse.json
network-pulse.json.prev
